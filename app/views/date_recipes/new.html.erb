<%= simple_form_for [@group_recipe, @date_recipe] do |f| %>
  <%= f.association :user, label_method: :first_name, class: "form-control"%>
  <%= f.input :date %>
  <%= f.submit class: "abtn abtn-lg abtn-prime" %>
<% end %>
